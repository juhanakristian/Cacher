Cacher is a simple app for finding geocaches.

Caches can be added by hand or by placing LOC-files to the applications
directory on the device. Cacher shows the caches on the device as
a simple list where the user can choose which cache they want to find.

When searching for a cache the application shows a compass which with
bearing and distance to the cache. The app also features a map
with car routing to the selected cache.

Find and download geocaches from geocaching.com
